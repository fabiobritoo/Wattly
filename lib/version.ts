// Bump this on every push that changes behavior/UI, so the user can
// confirm on their phone that the deploy that reached Vercel is the
// one they're looking at.
export const APP_VERSION = "3.1.0";
